# lazy-cli

A privacy-aware natural-language CLI assistant. The initial implementation focuses on static `argparse` inspection and local context collection without executing project code or transmitting environment-variable values.

## Development

```sh
python -m unittest discover -s tests
```
